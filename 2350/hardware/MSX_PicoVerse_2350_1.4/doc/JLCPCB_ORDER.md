# JLCPCB 발주 가이드 — MSX PicoVerse 2350 rev 1.4

원설계 The Retro Hacker / rev 1.3~1.4 개정 ESLAB
**개정** 2026-09-19

이 문서는 JLCPCB에 **PCB 제조**와 **선택적 SMT 실장**을 발주할 때 필요한 설정과
주의사항을 정리한 것이다. 파일은 모두 프로젝트 루트에 있다.

---

## 0. rev 1.3 → 1.4 변경 요약

| 부품 | 처리 | 이유 |
|---|---|---|
| **D2** (B5819WS) | **삭제** | USB VBUS 역류 차단 다이오드. 이것 때문에 MSX 구동 중 J5의 VBUS가 0 V가 되어 USB 메모리가 인식되지 않았다 |
| **SW1** (SK-12D02) | **삭제** | ESP 전원 선택 스위치 |
| **Q3** (DTC114EUA) | **삭제** | VBUS 검출. D2 제거로 VBUS가 상시 활성이 되어 검출 자체가 무의미 |
| **Q4** (SSM3J332R) | **삭제** | ESP 전원 스위치 P-FET |
| **R40** (10k) | **삭제** | Q4 게이트 풀업 |
| **C12** (0.1µF) | **삭제** | Q4 게이트 소프트스타트 |
| **F1** (FSMD075) | 역할 변경 | `+5V ↔ VBUS` 양방향 경로이자 **USB 호스트 전류 제한** 소자 |
| **U2** (UDA1334MOD) | **뒷면 → 앞면 이설** | B.Cu → F.Cu. 하판 셸 간섭 해소. 180° 회전 배치하여 3.5 mm 잭이 기판 좌측 외곽선 밖으로 3.13 mm 돌출 |

결과

```
rev 1.3 :  J5.VBUS ──D2──► F1 ──► +5V        (USB → 보드 단방향. 호스트 급전 불가)
rev 1.4 :  J5.VBUS ────── F1 ────── +5V      (양방향. MSX 5V가 USB 메모리를 구동)

ESP 전원 :  rev 1.3  SW1/Q3/Q4 경유 ESP_POW  →  rev 1.4  +3V3 상시
```

부품 수 110 → **104개**. 실장 97 / DNP 4 / 기판일체 3.

---

## 1. 발주 파일

| 파일 | 용도 |
|---|---|
| `gerbers/` 폴더를 zip 압축 | PCB 제조 (거버 + PTH/NPTH 드릴) |
| `MSX_PicoVerse_2350_1.4_JLCPCB_CPL.csv` | SMT 부품 좌표 — 97개 |
| `MSX_PicoVerse_2350_1.4_JLCPCB_BOM.csv` | SMT 부품 목록 — 36품목 (LCSC 번호 기입 필요) |
| `MSX_PicoVerse_2350_1.4_BOM.csv` | 한글 상세 BOM — 39품목. 발주용 아님, 조립 참고용 |

재생성

```
python3 tools/rev14_jlcpcb.py      # CPL + JLCPCB BOM
python3 tools/rev14_bom.py         # 한글 상세 BOM
```

두 스크립트 모두 `.kicad_pcb` 를 직접 읽는다. 외부 의존 없음.

> **거버와 CPL은 반드시 같은 시점의 보드에서 출력할 것.**
> 부품을 하나라도 옮긴 뒤에는 거버·드릴·CPL을 모두 다시 뽑아야 한다.
> 좌표만 갱신하고 거버가 옛것이면 전 부품이 어긋난 채로 실장된다.

---

## 2. PCB 제조 사양

| 항목 | 값 |
|---|---|
| 층수 | 4층 (F.Cu / In1.Cu / In2.Cu / B.Cu) |
| 외형 | 101.15 × 66.05 mm |
| 두께 | 1.6 mm (설계값 1.5982) |
| 스택업 | JLC04161H-3313A |
| 표면처리 | **ENIG** (무연 HASL 금지 — 아래 참조) |
| 골드핑거 | **있음, 45° 베벨** (J11 카트리지 엣지 커넥터) |
| 실크 | 양면 |
| 최소 선폭 | 0.20 mm |
| 최소 간격 | 0.15 mm |
| 최소 드릴 | 0.30 mm |
| 비아 테팅 | 앞뒤 모두 테팅 |

### 넷클래스

| 클래스 | 트랙 | 비아 (외경/드릴) |
|---|---|---|
| Default | 0.25 | 0.6 / 0.3 |
| Analog | 0.30 | 0.6 / 0.3 |
| Power | 0.60 | 0.8 / 0.4 |
| USB | 0.29 | 0.6 / 0.3 |

### 표면처리를 ENIG로 하는 이유

J11은 MSX 카트리지 슬롯에 직접 꽂히는 골드핑거다. HASL은 표면이 고르지 않아
삽입 시 슬롯 접점을 긁고, 반복 삽발 내구성이 떨어진다. 취미 프로젝트라도
슬롯에 꽂는 기판은 ENIG로 하는 편이 안전하다. JLCPCB에서는 골드핑거 옵션을
선택하면 해당 면이 별도 도금된다.

---

## 3. SMT 실장 발주

### 3.1 좌표계

이 프로젝트는 **보조 원점(aux origin)을 쓰지 않는다.** 거버가 절대좌표로 출력되며
CPL도 동일 원점을 쓴다.

```
Mid X =   KiCad X
Mid Y = −(KiCad Y)     ← KiCad는 Y 아래가 +, 거버/CPL은 Y 위가 +
Layer =  top / bottom
```

CPL 헤더는 JLCPCB 표준 그대로다: `Designator, Mid X, Mid Y, Layer, Rotation`
배치 범위는 X[−46.380 … 45.000], Y[13.190 … 64.800].

### 3.2 반드시 확인할 것

**① LCSC 부품번호를 채울 것**
`LCSC Part #` 열이 비어 있다. JLCPCB는 이 번호로 부품을 매칭하므로 비워두면
아무것도 매칭되지 않는다. 부품별로 LCSC에서 조회해 채워 넣는다.

**② 회전값을 미리보기에서 확인할 것**
JLCPCB 부품 라이브러리의 기준 각도가 KiCad와 다른 패키지가 많다.
특히 SOT-23, SOT-23-6, TSOT-26, SOD-323 계열은 90° 또는 180° 차이가 흔하다.
업로드 후 렌더링 미리보기에서 부품별로 눈으로 확인하고 보정한다.
이 단계를 건너뛰면 트랜지스터·다이오드가 뒤집혀 실장된다.

현재 회전값 분포: 0°:21개 / 90°:47개 / 180°:8개 / 270°:21개

**③ 뒷면 부품 없음**
rev 1.4 에서 U2 가 앞면으로 이설되어 **이 보드에는 뒷면 부품이 하나도 없다.**
CPL 의 `Layer` 열은 전부 `top` 이다. bottom 부품 회전 규약을 따질 필요가 없다.

### 3.3 CPL 수록 범위

CPL에는 **97개 부품**이 들어 있다. 아래는 제외되어 있다.

| 구분 | 부품 | 사유 |
|---|---|---|
| DNP | C16, J3, R43, R44 | 기본 미실장 |
| 기판 일체 | J11, PAD01, PAD02 | 골드핑거 / 마운팅홀 — 부품 아님 |

### 3.4 SMT로 실장할 수 없는 부품

CPL에는 들어 있지만 SMT 서비스로는 실장되지 않는다.
**주문 화면에서 해제하고 수동 납땜한다.**

| 부품 | 값 | 패드 구성 | 비고 |
|---|---|---|---|
| U1 | Core2350B | 스루홀 64 | 모듈 |
| U2 | UDA1334MOD | 스루홀 15 | 모듈, 앞면 |
| J2 | ESP-01 | 스루홀 8 | 2x4 헤더 |
| J5 | USB-C 16P | SMD 16 + 스루홀 4 | 혼합. 미리보기로 가능 여부 확인 |

> rev 1.3 의 SW1(슬라이드 스위치)은 삭제되어 목록에서 빠졌다.

J4(microSD)는 SMD 13핀 + NPTH 2개 구성이라 **SMT 실장 가능**하다.
나머지 93개는 순수 SMD로 실장 가능하다.

---

## 4. 수동 조립 주의사항

SMT를 맡기든 전부 직접 하든, 아래는 사람이 조립하는 부분이다.

### 4.1 모듈 (U1 / U2 / J2)

- **핀헤더 소켓(암) 사용 금지.** 소켓 높이 때문에 케이스를 닫을 수 없다.
- **U1 / J2** 는 핀헤더의 플라스틱 지지대(스페이서)를 제거한 뒤 조립한다.
  모듈이 기판에 밀착되어야 한다.
- **U2 는 반대다 — 밀착시키면 안 된다.** 부품면을 아래로 엎어 꽂으므로
  6.00 mm 스탠드오프가 필요하다. 아래 4.2 참조.

### 4.2 U2 (UDA1334A I2S DAC 모듈) — rev 1.4 에서 앞면으로 이설

- **rev 1.3 의 뒷면(B면) 실장에서 앞면(F면)으로 바뀌었다.** 이제 이 보드에
  뒷면 부품은 하나도 없다.
- **모듈은 부품면을 아래로 엎어서 꽂는다.** UDA1334ATS · 47µF 2개 · 3.5 mm 잭이
  2350 기판을 향하고, 핀헤더는 모듈의 **부품면**에서 나와 기판을 관통한다.
  위에서 보이는 것은 모듈의 민면이다.
- **스탠드오프 6.00 mm 가 필요하다.** 47µF 캔 5.40 mm 가 들어갈 공간이 있어야 한다.
  핀헤더 플라스틱 지지대 2.54 mm 로는 부족하므로 **긴핀 헤더(핀 길이 11 mm 이상)
  또는 별도 스페이서**를 쓴다.
- 핀 관통 길이 = 기판 아래 3.2 + 스탠드오프 6.0 + 모듈 1.6 ≈ **최소 10.8 mm**
- **3.5 mm 오디오 잭이 기판 좌측 외곽선 밖으로 3.13 mm 돌출한다.**
  잭 중심 높이는 기판면에서 약 3.5 mm. 케이스 잭 타공을 여기에 맞춘다.
- F.Silkscreen 의 1번 핀 표시를 확인할 것.

```
기판면 기준 Z (mm)
  -3.20 ─ 핀헤더 하단 (기판 관통)
   0.00 ─ 2350 기판 표면
   0.60 ─ 47µF 캔 바닥          ← 스탠드오프를 결정
   1.00 ─ 3.5mm 잭 바닥
   6.00 ─ 모듈 PCB 아랫면(부품면)
   7.60 ─ 모듈 PCB 윗면(민면)   ← 전고
  10.35 ─ 상판 셸 안쪽          ← 여유 2.75
```

> **하판 셸 간섭은 해소되었다.** 기판 위로 10.35 mm 가 있고 전고는 7.60 mm 다.
> 남은 과제는 잭 돌출부의 케이스 타공과 6 mm 스탠드오프 부품 확보다.

### 4.3 J5 (USB-C) — rev 1.4 에서 역할이 늘었다

- 펌웨어 업로드 포트이자 **USB 메모리(매스스토리지) 호스트 포트**를 겸한다.
- VBUS는 F1을 통해 `+5V`에서 **상시 공급**된다. 별도 전원 스위치가 없다.
- **★★ MSX에 카트리지를 꽂은 상태로 PC에 USB 케이블을 연결하지 말 것.**
  D2가 없으므로 MSX 5 V가 PC의 VBUS로 역류한다. 펌웨어 업로드는 카트리지를
  MSX에서 뺀 상태에서 한다. 자세한 근거는 `doc/USB_MSC_2350.md` §7.3.
- CC1/CC2는 Rd 5.1k 고정이라 USB-C 규격상 싱크(UFP) 배선이다. USB2 데이터에는
  영향이 없지만, "USB-C 규격 준수 호스트"로 표기하지는 말 것.

### 4.4 S1 (A06-B6-1 측면 푸시 스위치)

- 기판 상단 돌출부에 실장되며 액추에이터가 기판 외곽선보다 0.5 mm 바깥으로 나온다.
  케이스 버튼 타공·플런저 위치를 확인할 것.
- **위치결정 보스 Ø0.6 두 개가 기판의 Ø0.75 비도금 홀에 완전히 안착된 것을
  확인한 뒤 납땜한다.** 떠 있으면 측면 하중에 단자가 뜯어진다.
- 금속 커버 고정탭(패드 3·4)은 GND에 연결되어 있고 각 탭 중앙에 앵커 비아가 있다.
  수땜 시 배럴에 납이 차면서 리벳처럼 고정된다. 뒷면은 마스크로 덮여 있어
  납이 새어나오지 않는다.

### 4.5 J2 (ESP-01) — rev 1.4

- ESP-01 전원이 **+3V3 상시**로 바뀌었다. 전원 선택 스위치가 없으므로
  보드에 전원이 들어오면 ESP도 항상 켜진다.
- 스트래핑 풀업 R8 / R9 / R10 / R39 는 모두 **4.7k** 로 통일되었다.

### 4.6 J3 (SWD 헤더)

- 기본 **DNP**. 펌웨어 디버깅이 필요할 때만 실장한다.
- 실장할 경우 **라이트 앵글(수평) 헤더만** 사용한다. 수직형은 케이스에 간섭한다.

### 4.7 DNP 부품

| 부품 | 실장 조건 |
|---|---|
| C16 (100 µF) | WiFi 동작이 불안정할 때만 |
| R43 / R44 (10 k) | U2 모듈에 PLL / DEEM 풀다운이 없을 때만 |
| J3 (SWD) | 디버깅이 필요할 때만 |

### 4.8 대체 시 주의가 필요한 부품

한글 BOM의 ★ 표시 항목이다. 아래는 그중 특히 영향이 큰 것들이다.

| 부품 | 주의 |
|---|---|
| **F1 FSMD075-1812** | **★★ rev 1.4에서 역할이 바뀌었다.** D2 삭제로 F1이 `+5V ↔ VBUS`를 잇는 유일한 소자이며, 홀드 0.75 A / 트립 1.5 A가 곧 USB 호스트 전류 제한이다. **0 Ω 점퍼로 대체 금지** |
| Q1 DMMT5401 | 정합 PNP 쌍. 개별 PNP 2개로 대체 금지 — 정합도가 오링 동작의 핵심 |
| R6 196k / R7 62k | 1% 필수. 벅 출력 3.33 V를 결정한다 |
| R50 0Ω | GNDA ↔ GND 단일점 브리지. 반드시 실장 |
| R52 10Ω 0805 | 비드가 아니라 저항. C22와 함께 DAC 전원 RC 필터를 이룬다 |
| C22 10 µF 16 V 1206 | DC 바이어스 디레이팅 때문에 0603 / 6.3 V 대체 금지 |
| FB1 120Ω | 정격 1 A 이상. ESP TX 350 mA가 통과하므로 일반 200 mA 비드 금지 |
| L1 SPH5030-100M | 10 µH, Isat 1 A 이상, DCR 100 mΩ 이하 |
| IC1 AP63200WU-7 | 오디오에 주기적 잡음이 생기면 AP63201WU-7(강제 PWM)로 핀 호환 교체 가능 |
| TVS1 SM6T6V8CA | `+5V_MSX` 진입점. **VBUS 계통에는 TVS가 없다** — D2 삭제로 USB 입력 보호 소자가 사라졌다 |

---

## 5. 발주 전 체크리스트

- [ ] KiCad에서 **ERC / DRC 무오류** 확인
- [ ] 삭제된 6개(D2, SW1, Q3, Q4, R40, C12)의 **잔여 배선·비아가 없는지** 확인
- [ ] `/VBUS` 가 `F1.1` 로만 연결되어 있는지 넷리스트 확인
- [ ] **거버·드릴·CPL을 같은 시점에 다시 출력** (부품을 옮겼다면 필수)
- [ ] 거버 zip에 PTH / NPTH 드릴 파일이 모두 들어갔는지 확인
- [ ] JLCPCB 주문 화면에서 4층 / 1.6 mm / **ENIG** / **골드핑거 45° 베벨** 선택
- [ ] 스택업을 **JLC04161H-3313A**로 지정
- [ ] BOM의 `LCSC Part #` 전부 기입
- [ ] SMT 미리보기에서 **회전값 전수 확인** (특히 SOT-23 / SOD-323 / TSOT-26)
- [ ] U1 / U2 / J2 / J5 를 실장 목록에서 해제
- [ ] DNP 4개(C16, J3, R43, R44)가 실장 목록에 없는지 확인
