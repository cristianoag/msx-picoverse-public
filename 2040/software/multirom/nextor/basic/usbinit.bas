10 DEFINT A-Z
20 PRINT "USB INIT..."
30 OUT &H9E,&H01
40 GOSUB 2000
50 IF status<>0 THEN PRINT "INIT FAILED: ";HEX$(status):END
60 PRINT "INIT OK."
70 OUT &H9E,&H02
80 GOSUB 2000
90 IF status<>0 THEN PRINT "DESCRIPTOR REQ FAILED: ";HEX$(status):END
100 PRINT "READING DESCRIPTOR..."
110 desc$=""
120 FOR i=1 TO 255
130 b=INP(&H9F)
140 IF b=&HFF THEN GOTO 170
150 desc$=desc$+CHR$(b)
160 NEXT i
170 IF desc$="" THEN PRINT "NO DESCRIPTOR RETURNED.":END
180 PRINT "USB DEVICE: ";desc$
190 END
2000 status=&HFF
2010 FOR waitcnt=1 TO 1000
2020  status=INP(&H9E)
2030  IF status<> &HFF THEN RETURN
2040 NEXT waitcnt
2050 RETURN